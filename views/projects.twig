{% extends "base.twig" %}

{% block content %}
  {% include "partials/background-lines.twig" %}
  {% for project in projects %}
    <h2>{{ project.title }}</h2>
    <p>{{project.post_content}}</p>

  {% endfor %}
{% endblock %}