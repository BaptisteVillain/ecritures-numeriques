<section class="single__footer">
  <div class="single__footer__wrapper">
    <div class="single__footer__page--previous">
      <p class="page__label">{{__('précédent', 'ecritures-numeriques')}}</p>
      <a href="{{previous_path}}" class="page__title">{{previous_name}}</a>
    </div>
    <div class="single__footer__page--next">
      <p class="page__label">{{__('suivant', 'ecritures-numeriques')}}</p>
      <a href="{{next_path}}" class="page__title">{{next_name}}</a>
    </div>
  </div>
</section>