 {% extends "base.twig" %}

{% block content %}
	{% include "partials/background-lines.twig" %}
  <div class="publications">
    <header class="publications__header">
      <button class="header__nav header__nav--prev">
        <
      </button>
      <div class="header__wrapper">
        <ul class="header__list">
          <li class="header__item">
            <button class="{{term ? '' : 'item--active'}}" data-slug="">
            {{__('tout')}}
            </button>
          </li>
        {% for item in selected %}
          <li class="header__item">
            <button class="{{term.slug == item.slug ? 'item--active' : ''}}" data-slug="{{term.slug}}">
            {{ item.name }}
            </button>
          </li>
        {% endfor %}
        </ul>
      </div>
      <button class="header__nav header__nav--next">
        >
      </button>
    </header>
    <div class="publications__posts">
      {% for post in posts %}
        <div>
          {{post.title}}
        </div>
      {% endfor %}
    </div>
  </div>
{% endblock %}
